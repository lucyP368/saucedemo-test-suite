package com.lucyP368.pages;

import java.util.List;

import org.openqa.selenium.WebDriver;

public class HomePage extends Page {
    public HomePage(WebDriver driver) {
        super(driver, List.of());
    }
    
}
